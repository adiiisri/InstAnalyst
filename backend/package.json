{
  "name": "instanalyst-backend",
  "version": "1.0.0",
  "description": "Backend for InstAnalyst - Instagram Video Downloader & Analytics",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "postinstall": "pip install yt-dlp --break-system-packages || python3 -m pip install yt-dlp --break-system-packages || python -m pip install yt-dlp --break-system-packages || pip install yt-dlp || python3 -m pip install yt-dlp || python -m pip install yt-dlp"
  },
  "dependencies": {
    "@sasmeee/igdl": "^0.0.1-security",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.1",
    "youtube-dl-exec": "^3.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
